<div class="container">
  <div class="container">

    <%# top page profile section %>
    <div class="profile ">
    <%= link_to "Sign out", destroy_user_session_path, class: "sign-out", data: {turbo_method: :delete} %>
      <%= cl_image_tag @user.photo.key, class: "avatar box06403 mar10", height: 300, width: 400, crop: :fill %>
      <%# profile information: ie name %>
      <div class="profile-info">
        <h2><%= @user.username %></h2>
      </div>
    </div>

    <%# tabs for favourites and bookings section %>
    <div class="tabs">
        <div class="tab-pane fade show active" id="tabs" role="tabpanel" aria-labelledby="my-tab">
          <ul class="nav nav-tabs" id="myTab" role="tablist">

            <li class="nav-item" role="presentation">
              <button class="nav-link" id="favourites-tab" data-bs-toggle="tab" data-bs-target="#favourites" type="button" role="tab" aria-controls="favourites" aria-selected="true"><h2>Favourites</h2></button>
            </li>

            <li class="nav-item" role="presentation">
              <button class="nav-link active" id="bookings-tab" data-bs-toggle="tab" data-bs-target="#bookings" type="button" role="tab" aria-controls="bookings" aria-selected="false"><h2>Accepted</h2></button>
            </li>

            <li class="nav-item" role="presentation">
              <button class="nav-link" id="reviews-tab" data-bs-toggle="tab" data-bs-target="#reviews" type="button" role="tab" aria-controls="reviews" aria-selected="false"><h2>My reviews</h2></button>
            </li>

          </ul>
        </div>
      <%# tab content %>
      <div class="tab-content clearfix" id="myTabContent">
        <%# favourites tab %>
        <div class="tab-pane fade " id="favourites" role="tabpanel" aria-labelledby="favourites-tab">
          <%= render "favourites/tab" %>
        </div>
        <%# booking calender %>
        <div class="tab-pane fade show active" id="bookings" role="tabpanel" aria-labelledby="bookings-tab">
          <%= render "bookings/tab" %>
        </div>
        <%# reviews/past events tav %>
        <div class="tab-pane fade show" id="reviews" role="tabpanel" aria-labelledby="reviews-tab">
          <%= render "reviews/my_reviews" %>
        </div>
      </div>
    </div>

  </div>

  <%= render "shared/navbar" %>

</div>
